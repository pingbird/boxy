name: boxy
description: Create advanced multi-child layouts in Flutter.
version: 1.0.0
homepage: https://github.com/PixelToast/flutter-boxy

environment:
  sdk: '>=2.7.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter